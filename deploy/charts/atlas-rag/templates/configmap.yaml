apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "atlas-rag.fullname" . }}
  labels:
    {{- include "atlas-rag.labels" . | nindent 4 }}
data:
  {{- range $name, $value := .Values.config }}
  {{ $name }}: {{ $value | quote }}
  {{- end }}
